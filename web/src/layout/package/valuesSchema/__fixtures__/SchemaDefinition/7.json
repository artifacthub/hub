{
  "def": {
    "active": 0,
    "combinationtype": null,
    "options": [
      {
        "enum": ["trace", "debug", "info", "warn", "error", "fatal", "panic"],
        "type": "string",
        "title": "Log level",
        "default": "info"
      }
    ]
  },
  "defaultValue": "value"
}
